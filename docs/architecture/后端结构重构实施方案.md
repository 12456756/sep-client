# 客户端后端结构重构实施方案

> 适用范围：Electron 主进程后端（`electron/` + `pi-extension/`），共 45 个文件、约 6105 行 TypeScript。
> 本轮不改动渲染进程（`src/`）代码与 IPC 契约。
>
> **本文档是行为基准。** 原有的《后端结构重构方案》与《客户端后端任务运行框架方案》已删除且不再遵循，
> 并发规则、状态机语义、错误码语义此前只存在于代码中。第 2 章的缺陷清单与第 7 章的并发不变式
> 共同承担基准职责，Phase 0 必须先把它们落成可执行断言。

---

## 0. 设计原则与工作准则

### 0.1 三条设计原则

1. **每个模块只有一个改变的理由。** 判断标准不是文件大小，而是"一次需求变更会不会同时改它的两个部分"。
2. **依赖单向流动。** 控制层 → 服务层 → {数据层, 执行层} → 公共层。反向引用一律禁止，用接口 + 注入倒置。
3. **每种判断只有一个定义点。** 本次重构最大的收益来源：当前 scope 校验有 10 份、副作用工具清单有 3 份、
   脱敏正则有 2 份、原子写有 3 份、路径推导有 4 份。

### 0.2 四条工作准则

| # | 准则 | 含义 |
|---|------|------|
| W1 | **冗余代码直接删除，不予保留** | 已确认无引用的代码不搬迁、不注释保留、不留 `@deprecated` 兼容层。归并重复实现时删掉旧实现，不留 re-export shim。清单见第 6 章。 |
| W2 | **使用标准日志打印，在需要日志的地方设置** | 全后端只有一个日志入口 `common/logger.ts`，禁止裸 `console.*`。日志点由第 5 章的规范决定，不靠个人习惯。 |
| W3 | **先修缺陷，后做重构** | Phase 1 在**结构一行不动**的前提下修完第 2 章全部 9 个缺陷。结构调整从 Phase 3 才开始。 |
| W4 | **单次提交只做一件事** | 任何提交不得同时改结构和改行为。行为修正单独成 commit 并在信息里写明修的是哪个缺陷编号。 |

W3 是本方案与初版的最大差别。理由：C1（任务永久卡死）、C2（工作区锁永久泄漏）、C3（停机数据丢失）、
C5（审批可能批准错误的工具调用）是**当前已存在的线上缺陷**，与重构无关。让它们等待一轮多阶段重构才修复
不合理；且它们一旦在重构中期发作，会与结构改动的回归混淆，难以定位。

---

## 1. 现状问题总览

按你提出的三个关注点归类。

### 1.1 控制层不成层

`electron/main.ts` 837 行，27 个 `ipcMain.handle` + 1 个 `ipcMain.on` 全部写在模块顶层：

- **不可测**：注册发生在 import 副作用里，测试无法在不启动 Electron 的情况下调用任何一个 handler。
- **10 个模块级可变全局**：`mainWindow`、`taskCoordinator`、`taskCoordinatorInitialization`、`taskManager`、
  `taskRunStore`、`workflowStore`、`taskMetadataStore`、`activeInstances`、`subscriptionRuntime`、
  `authenticationCleanupPromise`。加 6 个手写惰性单例（`ensureXxx`）维护它们。
- **3 种错误信封**：`authError()`（`main.ts:66`）、`taskError()`（`main.ts:144`）、以及 `main.ts:413`
  一处裸对象 `{ message, statusCode: 401 }` 和 `main.ts:430` 的 `{ statusCode: 0 }`。
- **约 10 处逐字复制的 scope 守卫**。
- **`task:create`（438 行）与 `conversation:create`（583 行）近乎重复**。
- 参数校验靠人记得写，没有结构强制。

### 1.2 服务层完全缺失

用例逻辑散落在 IPC 回调内部，没有一个可以脱离 Electron 调用的业务层。最典型的越层：
`TaskManager` 同时是任务仓库、状态机执行者，**并且直接持有 `BrowserWindow` 发 IPC**
（`task-manager.ts:415-426` 的 `notifyTaskUpdate` / `notifyTaskListUpdate`）。一个服务类知道渲染进程的存在。

### 1.3 数据层不成层

4 个 store（`task-store`、`task-run-store`、`workflow-store`、`task-metadata-store`）各自为政：

- **3 套独立的原子写实现**（tmp + rename），行为细节不一致：`task-store` 有 `.bak` 备份与 `quarantine`，
  `workflow-store`（33 行）什么都没有。
- **路径推导重复 4 处**，`workflow-store.ts:11` 自己拼 `task-data/v3/<ent>/<mem>/tasks/<id>/workflow.json`。
- **`task-run-store.ts` 461 行捆了 5 件事**：run 记录、事件日志、消息投影、崩溃恢复、路径推导。
- 脱敏正则 `SENSITIVE_KEY` 与 `sanitizeError`/`sanitizeValue` 在 `task-run-store.ts` 与
  `pi/sdk/pi-coding-agent-adapter.ts:30` 各有一份。

### 1.4 文件夹按来源命名而非按职责

`auth/`、`tasks/`、`pi/`、`runtime/`、`infrastructure/` 混合了控制、服务、数据、领域逻辑。
`tasks/` 里同时有 store、manager、coordinator、domain。新人无法从目录名推断一段逻辑该放哪。

### 1.5 扩展成本实测

以"给任务加标签"为例，今天需要改：`src/shared/types.ts`、`src/shared/ipc.ts`、`preload.ts`、
`main.ts`（handler + 参数校验 + scope 校验 + 错误映射，约 20 行）、store，
共 **6 个文件、4 个必改，`main.ts` 再长 20 行**。重构后的目标见第 9 章。

---

## 2. 缺陷清单（Phase 1 修复对象）

逐行审查调度、锁、事件链、状态提交四条并发路径后确认的 9 个缺陷。C1/C2/C3 会丢任务或丢数据，
C5 涉及安全约束。**每条独立成 commit**，commit 信息注明编号。

### C1 — `pump()` 按下标 splice 队列，会丢掉无关的排队任务【严重】

`electron/tasks/task-execution-coordinator.ts:297-318`：

```ts
for (let index = 0; index < this.queue.length;) {
  const queued = this.queue[index]
  const task = await this.taskManager.getTask(queued.taskId)            // 挂起点 1
  if (!task || task.activeRunId !== queued.runId) {
    this.queue.splice(index, 1); continue
  }
  const employee = await this.authorizeEmployee(queued.subscriptionId)  // 挂起点 2（网络）
  if (!employee) { this.queue.splice(index, 1); /* ... */ }
  // ...
  this.queue.splice(index, 1)
  void this.startRun(/* ... */)
}
```

`queued` 在 `index` 处捕获，但经过两个 await 后 `this.queue` 可能已被改动——`cancelTask:241` /
`pauseTask:224` 调用的 `removeQueuedTask()` 是**同步 splice**，正好能在 `pump` 挂起时插进来。
恢复执行后 `splice(index, 1)` 删掉的是**另一个**条目。

**后果**：被误删条目对应的 task 仍持有 `activeRunId`，但它永远不会被启动，也永远不会被清理。
任务卡在 PENDING 且无法重试（`admitTask:188` 见 `activeRunId` 非空即抛 `TaskAdmissionError`）。
用户看到一个永久卡死、无法操作的任务。

**修法**：队列条目改为按 `runId` 寻址。取出用 `findIndex(e => e.runId === runId)`，
`removeQueuedTask` 同样按 runId 操作。Phase 1 阶段不拆类，只把下标寻址换成 runId 寻址。

### C2 — 锁获取与 try/finally 之间有裸露区，会永久泄漏工作区锁【严重】

`task-execution-coordinator.ts:350-402`：`releaseWorkspace` 由 `pump():311` 获得后传入 `startRun`，
但 `startRun` 内的 `getPaths()`（360-365）与 `createWorker()`（366-395）**都在 try 之外**。

- `getPaths()` 对非法 taskId/runId 抛 `TaskScopeError`（`task-run-store.ts:374-375, 386`）。
- `createWorker()` 构造 `PiTaskWorker`，同样可能抛。

任一抛出，`finally` 从未进入 → `releaseWorkspace()` 永不调用 → **该工作目录被永久锁死**。
之后所有落在同一目录（含父子目录，`pathsOverlap` 会判定重叠）的任务在 `pump` 里永远 `acquire` 失败，
静默滞留队列，无任何日志。且 `activeByTask` 已在 401 行 set，此后不会被清理。

**修法**：锁的获取与释放必须在同一个词法块内。`startRun` 改为进入函数后立即
`try { /* 全部建仓步骤 */ } finally { releaseWorkspace() }`，不留裸露区。

### C3 — `will-quit` 是同步事件，`stopAll()` 根本没跑完【严重】

`main.ts:833-837`：

```ts
app.on('will-quit', async () => {
  if (taskCoordinator) { await taskCoordinator.stopAll() }
})
```

Electron 不 await `will-quit` 监听器的返回值。进程在 `stopAll()` 的第一个 await 处就继续退出。

**后果**：

- 正在执行的 `bash`/`write`/`edit` 的 `SIDE_EFFECT_UNKNOWN` 事件不会落盘
  （`finalizeRun:487-500` 专门处理这段），下次启动无法判定副作用是否已发生。
- worker 不 dispose，pi 会话文件可能半写。
- `markActiveRunsInterrupted`（`task-run-store.ts:250`）下次启动虽能兜底，
  但拿不到进程内的 in-flight 信息，恢复精度下降。

同一处还有孪生问题：`main.ts:816` 的 `ensureTaskManager()` 未 await，rejection 无人处理。

**修法**：改用 `before-quit` + `preventDefault` 的两阶段退出，并给停机预算设上限：

```ts
let shuttingDown = false
app.on('before-quit', event => {
  if (shuttingDown) return
  event.preventDefault()
  shuttingDown = true
  void withTimeout(stopAllAndDispose(), SHUTDOWN_BUDGET_MS)
    .finally(() => app.exit(0))
})
```

`SHUTDOWN_BUDGET_MS` 建议 5000。必须有界，理由见 C7。

### C4 — `authorizeEmployee` 在调度循环内发网络请求，会阻塞全局准入

`pump():304` 每轮对每个排队条目调一次 `authorizeEmployee`，而它（`main.ts:171-186`）内部是
`getValidAccessToken()` + `GET /client/subscriptions` + `SubscriptionRuntime.prepare()`。
这段代码运行在 `pumping = true` 期间：

- 一次慢或挂起的平台请求 → **所有任务的准入全部停摆**，且没有超时保护。
- `executeTask:137` 已调过一次，`pump` 又调一次 → 每个 run 至少 2 次平台往返；
  因锁冲突重试时还会再调。

叠加时序耦合：`authorizeEmployee` 写全局 `activeInstances`，而注入给 coordinator 的
`resolveEmployee`（`main.ts:155`）读同一个全局。`resolveEmployee` 的返回值取决于
"最后一个写 `activeInstances` 的人是谁"（可能是 `authorizeEmployee`，也可能是 `auth:get-instances`）。
这是时序耦合，不是数据流。

**修法**：Phase 1 只做能力拆分，不建新目录——授权在**入队前**完成一次，
`EmployeeRuntimeConfig` 随队列条目携带，调度循环内改为只读快照，**禁止任何网络调用**；
平台目录读取加 TTL 缓存与单飞（singleflight）去重。Phase 5 再把这两件事正式落成
`service/employee-directory.ts` 与 `service/employee-authorizer.ts`，届时全局 `activeInstances` 删除。

### C5 — 审批响应可能批准错误的工具调用【安全相关】

`electron/pi/approval-broker.ts:46-51`：

```ts
respond(response) {
  let requestId = response.requestId
  if (!requestId && this.pending.size === 1) requestId = this.pending.keys().next().value
  // ...
}
```

场景：请求 A 在 60s 超时被自动拒绝（`:36-38`），紧接着请求 B 进入 pending。
用户此时点了针对 A 的"批准"按钮，而渲染进程送来的响应缺 `requestId`
（`ToolApprovalResponse.requestId` 在 `src/shared/ipc.ts:88` 是可选字段）→ 命中 `size === 1` 分支
→ **B 被批准**。B 可能是一个 `bash`。

这违反项目约束"所有 `bash`/`write`/`edit` 需显式用户批准"——用户批准的不是这一个。

**修法**：`requestId` 必填。缺失时只在"恰好一个 pending **且** taskId/runId 匹配"时才回退，
否则返回 false 并记 warn 日志。

### C6 — 事件序号分配是 O(n²) 文件读，且失败被静默吞掉

`task-run-store.ts:350-371` 的 `appendEvent` 每追加一条事件都调 `getTimeline()`——
完整读取并逐行 `JSON.parse` 整个 `events.jsonl`，只为算下一个 sequence 并做冲突检查。
而 `text_delta` 是**逐 token** 产生的。一个产生 2000 个 delta 的 run 会做 2000 次全文件读取，
且全部串在同一条 `writeChains` 链上，直接拖慢事件到达渲染进程的延迟。

同时 `enqueueEvent`（coordinator:460-469）的链条是 `prior.catch(() => undefined).then(...)`，
`appendEvent` 抛出的 `TaskScopeError` 被完全吞掉：事件既没落盘、没到渲染进程，也没有任何日志。

还有一处：`startRun:433` 用 `await (this.eventChains.get(taskId) ?? Promise.resolve())` 等待事件排空，
但 `enqueueEvent:465-467` 的 `finally` 会在链末尾删掉 map 条目，所以这里可能拿到 `undefined` 而立即返回
——**没有真正的"排空"语义**。

**修法**：持有 per-(task, run) 的内存序号计数器，首次访问时从文件尾部 seek 一次初始化，之后纯内存自增。
链条错误交给日志与错误上报，不再吞。提供显式的 `drain(taskId): Promise<void>`，语义明确。

### C7 — 停机与认证失效链路无界，可以等到 60 秒

`stopAll()`（coordinator:264-282）串行 `await active.worker.abort()` + `await active.completion`。
run 内部可能正卡在 `approvalBroker.request()` 的 60s 定时器上。虽然 `stopAll` 第一行就 `denyAll()`
解除审批等待，但 `enqueueEvent` 的落盘链在慢磁盘上仍无上限。

`invalidateAuthentication`（`main.ts:188-205`）同样：它内部 `await taskCoordinator.stopAll()`，
但**没有任何调用方 await 它返回的 promise**。401 触发后，IPC 处理器仍在用半清理状态继续服务
（`activeInstances` 已清空，但 `taskManager` 的 scope 还没清）。

**修法**：`stopAll` / 停机 / 认证失效全部套 `withTimeout`，超时后走强制路径并记 error 级日志。
认证失效引入显式状态：进入 invalidating 后，scope 守卫对所有需要 scope 的用例直接返回 `AUTH_REQUIRED`，
而不是让它们读到半清理的中间状态。

### C8 — `setCurrentUser` 不走 `mutationChain`，登录切换存在写入丢失窗口

`task-manager.ts:81-126` 的 `setCurrentUser` 直接 `this.currentUser = null` / `this.tasks.clear()` /
`this.tasks = new Map(loadedTasks...)`，全程**不经过** `commit()` 的 `mutationChain`（`:380-392`）。

一个在途的 `commit` 已在 `submit` 外捕获了 `scope`（`:381` 的 `requireCurrentUser()`），
并在闭包里基于**旧** `this.tasks` 计算 `nextTasks`。若它在 `setCurrentUser` 之后才落盘，
就会把旧快照写回旧 scope 的文件——**丢掉切换前最后一批状态变更**。

因为路径按 scope 编码（`task-store.ts:261-265`），这是数据丢失窗口，不是跨租户泄漏。
但登出或 401 时正有任务在跑是完全现实的路径（`invalidateAuthentication` → `clearCurrentUser`）。

**修法**：scope 切换本身作为 `mutationChain` 上的一次操作；引入 `generation` 计数，
`commit` 的 `submit` 内部先校验 generation 未变，否则放弃写入并记 warn 日志。

### C9 — 检查与提交分离（TOCTOU），错误码失真

`task-manager.ts` 混用了两种写法：

- `admitTask`（`:183-195`）、`settleTaskRun`（`:221-242`）把守卫放在 `commit` 的 mutate 闭包**内部**
  ——正确，串行化保护生效。
- `pauseTask`（`:312-324`）、`cancelTask`（`:326-343`）、`deleteTask`（`:345-351`）、
  `addTaskFile`（`:291-301`）先在**链外**读状态判断，再进链修改——两次判断之间状态可变。

实际后果不是数据损坏（闭包内还有 `assertTaskTransition` 兜底），而是**错误码失真**：
链外判断通过、链内 `assertTaskTransition` 抛 `InvalidTaskTransitionError`，被映射成 `INTERNAL_ERROR`
（「操作未能完成。」），而用户实际遇到的是"当前状态下无法执行该操作"。用户看到一个假的内部错误。

**修法**：统一为"守卫只在 mutate 闭包内"一种形态，唯一检查点。
`InvalidTaskTransitionError` 在错误映射里对应 `INVALID_STATE`。
顺带清掉 `task-manager.ts:338-339` 的死赋值（`nextTask.error` 被赋值后立刻被下一行覆盖）。

### 已知限制（本轮不修，仅记录）

- `WorkspaceLockManager` 是纯内存实现（`HeldLock` 不落锁文件），**无法跨进程互斥**；
  代码里也没有 `requestSingleInstanceLock`。两个 Electron 实例共享同一 `userData` 会互相破坏。
  **因此两个工作树不能同时 `npm run dev`。**
- `common/platform/credential-vault.ts`（原 `credentials.ts`）在模块作用域用同步 fs，
  且 import 时就读 `app.getPath('userData')`，Electron 外不可测。它在登录路径上，回归风险最高，
  不混进结构重构，留待下一轮。

---

## 3. 目标结构

### 3.1 依赖方向

```
                    ┌─────────────────┐
   renderer ──IPC──▶│   controller/   │  控制层：校验 → 调服务 → 转信封
                    └────────┬────────┘
                             │ 只能向下
                    ┌────────▼────────┐
                    │    service/     │  服务层：用例编排 + 权限
                    └──┬───────────┬──┘
                       │           │
              ┌────────▼──┐   ┌────▼──────┐
              │   data/   │   │ runtime/  │  数据层 / 执行层
              └────────┬──┘   └────┬──────┘
                       │           │  runtime 单向依赖 pi/sdk
                       │      ┌────▼──────┐
                       │      │  pi/sdk/  │
                       │      └────┬──────┘
                    ┌──▼───────────▼──┐
                    │ domain/ errors/ │  纯逻辑 / 报错
                    │     common/     │  平台连接 / 基础设施
                    └─────────────────┘
```

### 3.2 目录布局

```
electron/
├── main.ts                     ≤120 行：polyfill → 组装 → 注册 → 生命周期
├── bootstrap/
│   ├── composition-root.ts     createBackend()：替换 10 个全局 + 6 个 ensureXxx
│   ├── main-window.ts          BrowserWindow 创建与显示
│   └── shutdown.ts             有序停机（C3）
│
├── controller/         【控制层】
│   ├── channels.ts             33 个 channel 常量的唯一定义处
│   ├── router.ts               表驱动注册 + zod 校验 + 统一 catch + 统一信封
│   ├── context.ts              RequestContext：scope + 各服务句柄
│   └── routes/
│       ├── auth.routes.ts          6 条
│       ├── task.routes.ts          13 条
│       ├── conversation.routes.ts  2 条
│       ├── workflow.routes.ts      4 条
│       └── system.routes.ts        2 条 + 事件推送注册
│
├── service/            【服务层】不碰 Pi 对象、不发 IPC、不拼文件路径
│   ├── task-service.ts
│   ├── conversation-service.ts
│   ├── workflow-service.ts
│   ├── employee-directory.ts    员工目录 TTL 缓存（唯一的平台目录读取点）
│   ├── employee-authorizer.ts   纯授权：目录快照入参 → 结果出参，零全局
│   └── scope-guard.ts           唯一 scope 校验点（归并 10 处）
│
├── data/               【数据层】只认 scope 与数据，不决定能否运行
│   ├── atomic-file.ts           唯一原子写实现（归并 3 套）
│   ├── scope-path.ts            唯一路径推导（归并 4 处）
│   ├── task-store.ts
│   ├── task-run-store.ts        只留 run 记录
│   ├── task-event-store.ts      事件日志 + 内存序号分配（C6）
│   ├── task-message-projector.ts 消息投影
│   ├── task-metadata-store.ts
│   ├── workflow-store.ts
│   └── conversation-session-store.ts
│
├── domain/             〔纯逻辑，零 IO〕
│   ├── task-state-machine.ts
│   ├── workflow-graph.ts
│   └── conversation-context.ts
│
├── runtime/            〔执行与调度〕不知道 IPC 存在
│   ├── task-execution-coordinator.ts   退化为编排者
│   ├── admission-queue.ts       准入队列，runId 寻址（C1）
│   ├── worker-registry.ts       worker 生命周期 + 完成信号（C2）
│   ├── event-pipeline.ts        事件串行化 + drain() + 副作用追踪（C6）
│   ├── workspace-lock-manager.ts
│   ├── approval-runtime.ts      原 approval-broker（C5）
│   └── task-notifier.ts         唯一 IPC 推送出口（接口，实现由 bootstrap 注入）
│
├── errors/             ★ 报错模块
│   ├── error-codes.ts           唯一错误码表
│   ├── app-error.ts             AppError + 中文语义工厂
│   ├── error-mapper.ts          unknown → 信封（唯一映射点）
│   └── error-reporter.ts        脱敏上报 + 进程级兜底 + 致命错误弹框
│
├── common/             ★ 平台连接 + 基础设施（叶子层）
│   ├── platform/                与 SEP 平台通信（原 auth/）
│   │   ├── platform-api.ts          原 auth-api.ts
│   │   ├── auth-session-manager.ts
│   │   ├── instance-token-manager.ts
│   │   ├── credential-vault.ts      原 credentials.ts
│   │   └── device-fingerprint.ts
│   ├── logger.ts                唯一日志入口（W2）
│   ├── redact.ts                唯一脱敏实现（归并 2 处）
│   ├── with-timeout.ts          有界等待（C3/C7）
│   ├── config.ts / constants.ts
│   └── undici-polyfill.ts       必须保持首个副作用 import
│
└── pi/sdk/             SDK 类型与生命周期只允许出现在这里
    ├── pi-coding-agent-adapter.ts
    ├── pi-task-worker.ts
    ├── shared-session-adapter.ts
    └── subscription-resource-loader.ts   原 runtime/subscription-runtime.ts
```

`auth/` 归入 `common/platform/`：它不是给客户端前端用的，是与平台建立连接的通道，属基础设施。
本轮**只搬不改**（`git mv` + 改名 + 改 import），内部结构不动。

### 3.3 四条硬边界

`npm run lint` 当前在仓库范围内不可用（ESLint 9 需要 `eslint.config.*`，仓库里是 `.eslintrc.*`），
因此边界用一个约 20 行的独立脚本检查 import，纳入验收。

| # | 规则 | 消灭的现存违规 |
|---|------|----------------|
| B1 | `controller/` 之外不得出现 `ipcMain` / `ipcRenderer` / channel 字符串（唯一例外：`runtime/task-notifier.ts` 的接口定义） | `task-manager.ts:415-426` 持有 `BrowserWindow` 直接 `webContents.send` |
| B2 | `service/` 不得 import `pi/` 或 `@earendil-works/*` | 用例逻辑与 SDK 生命周期缠在 `main.ts` |
| B3 | `data/` 不得 import `service/` 或 `runtime/` | 数据层不参与"能不能跑"的判断 |
| B4 | `common/` 不得 import 上面任何一层 | `runtime/subscription-runtime.ts` 绕过 `auth-api` 自发裸 fetch |

---

## 4. 报错模块

### 4.1 错误码表（唯一定义点）

`errors/error-codes.ts`。码 → 中文文案 / HTTP 状态 / 可重试 / 日志级别，一张表决定四件事。

```ts
export const ERROR_CODES = {
  AUTH_REQUIRED:                 { status: 401, retryable: false, level: 'warn',  message: '请重新登录。' },
  INVALID_ARGUMENT:              { status: 400, retryable: false, level: 'warn',  message: '请求参数不合法。' },
  NOT_FOUND:                     { status: 404, retryable: false, level: 'warn',  message: '未找到对应资源。' },
  INVALID_STATE:                 { status: 409, retryable: false, level: 'warn',  message: '当前状态下无法执行该操作。' },
  CONFLICT:                      { status: 409, retryable: true,  level: 'warn',  message: '工作目录被其他任务占用，请稍后重试。' },
  PERSISTENCE_ERROR:             { status: 500, retryable: true,  level: 'error', message: '数据保存失败。' },
  EMPLOYEE_PACKAGE_UNAVAILABLE:  { status: 404, retryable: false, level: 'warn',  message: '所选员工的技能包不可用，请联系管理员发布已订阅的版本。' },
  EMPLOYEE_UNAVAILABLE:          { status: 404, retryable: false, level: 'warn',  message: '所选硅基员工已不可用。' },
  SESSION_UNRECOVERABLE:         { status: 409, retryable: false, level: 'warn',  message: '会话文件已损坏且无法恢复。' },
  RECOVERY_CONFIRMATION_REQUIRED:{ status: 409, retryable: false, level: 'warn',  message: '会话文件已损坏，确认后将依据任务历史重建对话。' },
  NETWORK_ERROR:                 { status: 0,   retryable: true,  level: 'warn',  message: '网络连接失败，请检查网络后重试。' },
  RATE_LIMITED:                  { status: 429, retryable: true,  level: 'warn',  message: '操作过于频繁，请稍后重试。' },
  SERVICE_UNAVAILABLE:           { status: 503, retryable: true,  level: 'error', message: '服务暂时不可用，请稍后重试。' },
  INTERNAL_ERROR:                { status: 500, retryable: false, level: 'error', message: '操作未能完成。' },
} as const

export type ErrorCode = keyof typeof ERROR_CODES
```

两种文案严格分开：

- **`message`（中文，进 IPC，用户可见）**：来自上表，或由 `AppError` 覆写。
- **`cause` / `details`（英文技术信息，只进日志）**：**绝不进 IPC 载荷**。
  这是项目约束"token 值不得出现在日志、控制台或 IPC 事件载荷"的落点。

### 4.2 抛什么 vs 谁翻译

现有 8 个 Error 类**全部保留原位**：`InvalidTaskTransitionError`、`TaskAdmissionError`
（`domain/task-state-machine.ts:13,25`）、`WorkflowGraphError`（`domain/workflow-graph.ts:24`）、
`ConversationRecoveryError`（`conversation-recovery-error.ts:3`）、`TaskPersistenceError`、
`TaskScopeError`（`task-store.ts:20,27`）、`AuthApiError`（`auth-api.ts:209`）、
`AuthenticationRequiredError`（`auth-session-manager.ts:13`）。

领域层不该知道错误码，数据层不该知道 HTTP 状态。改的是**谁负责翻译**：
现在是 `authError()` + `taskError()` + 一处裸对象共三份；之后只有 `error-mapper.ts` 一份。
`router.ts` 的 catch 调它，业务代码里不再出现任何手写 `{ success: false, error: {...} }`。

顺带收敛一个类型漏洞：`taskError()` 返回 `{ code: string }`（宽类型），所以它能吐出
`EMPLOYEE_PACKAGE_UNAVAILABLE` 与 `ConversationRecoveryError.code`，
而 `src/shared/types.ts:138` 的 `TaskErrorCode` union 只有 6 个成员、根本不含这些。
收敛到 `ErrorCode` 后，此类漂移在编译期即可发现。

> 本轮不改 `src/shared/types.ts`（忽视前端）。实施时用 grep 确认渲染进程是否对错误 `message`
> 做英文字符串匹配；若有，单独列出这些前端位置交你决定，不擅自改动。

### 4.3 error-reporter

- 日志级别由错误码表决定，不由调用方临时决定。
- 全部内容强制过 `common/redact.ts`。
- **接管进程级兜底**：注册 `process.on('unhandledRejection')` 与 `process.on('uncaughtException')`
  ——当前完全没有注册。
- **致命错误可见**：初始化失败走 `dialog.showErrorBox` 中文提示，而不是留一个"能打开但坏掉"的窗口。

### 4.4 编码前置结论（已完成，2026-09-01）

**结论：构建链是干净的，中文文案可以放心落地。** 乱码是一次性的历史事故，损坏范围已完全定界，
且原文全部可从 git 恢复。

#### 判定过程

1. **源文件本身都是合法 UTF-8**（无 BOM，CRLF）。乱码不是文件编码错误，
   而是**文字内容已被替换成错字**——UTF-8 字节被按 GBK 解读后，以新字符重新存回 UTF-8。
   验证：`'选择工作目录'.encode('utf-8').decode('gbk')` 精确得到源码里的 `'閫夋嫨宸ヤ綔鐩綍'`。

2. **构建链无关**。`npm run build` 后 `out/main/index.js`、`out/main/chunks/*.js`、
   `out/preload/index.mjs`、`out/renderer/assets/*.js` 全部是合法 UTF-8；
   源码里正确的中文（如 `config.ts` 的 `'主板序列号需要额外的原生模块。'`）在产物中**逐字不变**；
   源码里已损坏的中文在产物中保持同样的损坏形态。electron-vite / Rollup 全程按 UTF-8 透传，
   不做任何转码。**渲染进程产物零乱码。**

3. **损坏时点唯一**：`git log` 逐 commit 统计显示，`763e51e` 及之前所有版本的中文完全正确
   （PUA 字符数为 0），乱码在 `6006e9f`（`feat(enterprise): add silicon-employee workspace...`，
   2026-09-01 14:52）一次性出现。是那次提交的编辑器/工具以 GBK 读入了 UTF-8 文件，
   不是持续性的环境问题。

#### 损坏范围（全仓库精确扫描，共 5 个文件 25 处运行）

判定方法：非 ASCII 连续片段若能按 GBK/cp936 编回字节、再当 UTF-8 读出像源码的中文，
或含私用区（PUA）/片假名字符，即为乱码。**必须把 `€`→0x80 加进 GBK 反查表**——
Python 的 `gbk` 编解码器不含欧元符号，少了这一条会漏掉全部 `─` 分隔线
（`─` 的 UTF-8 是 `E2 94 80`，被 cp936 读成 `鈹€`）。首轮统计的"18 处"就是这样漏的。

| 文件 | 位置 | 性质 |
|------|------|------|
| `electron/main.ts` | 文件头（4/6/7/10 行，且与新写的头部重复了一遍） | 注释 |
| `electron/main.ts` | 229、292、294、395、434、785、811 | `─` 分隔线，无语义文字 |
| `electron/main.ts` | **795** | **用户可见**：目录选择框标题，应为「选择工作目录」 |
| `electron/main.ts` | 815 | 注释 |
| `electron/tasks/task-execution-coordinator.ts` | 576、580 | **用户可见**：任务日志文案，应为「执行工具: 」「工具执行完成: 」 |
| `electron/tasks/task-manager.ts` | 298、338 | **用户可见**：任务日志与错误文案，应为「生成文件: 」「用户取消」 |
| `src/shared/types.ts` | 2、3、309、315 | 注释 |
| `src/shared/types.ts` | 6、62、194、233、287、321 | `─` 分隔线 |

`main.ts:229` 与 `types.ts:2` 另有一个字被吃掉换成了 `?`（原文字节数为奇数，
末字节在 GBK 双字节切分下落单）。这两处不能靠反向编码复原，直接取 `763e51e` 的原文。

#### 原文可从 git 精确恢复

`763e51e` 版本保留了全部正确原文，无需猜测：

| 位置 | 恢复为 |
|------|--------|
| `main.ts:797` | `'选择工作目录'` |
| `coordinator:576` | `` `执行工具: ${...}` `` |
| `coordinator:580` | `` `工具执行完成: ${...}` `` |
| `task-manager.ts:298` | `` `生成文件: ${filePath}` `` |
| `task-manager.ts:338` | `'用户取消'`（该行本身是死赋值，按第 6 章直接删除，不必恢复） |
| `types.ts:2/3/309/315` | `'主进程与渲染进程共享的类型定义'` / `'IPC 通道两侧均引用此文件'` / `'高危工具调用，等待用户批准（文档 §6.2 措施 ④）'` / `'人读说明，如"写入文件 ~/Documents/..."'` |

#### 对本方案的影响

- **第 2 阶段的中文错误文案可以正常落地**，无需额外的编码方案或转码步骤。
- Phase 0 的编码任务从"判定成因"降级为"按上表恢复原文"，工作量确定。
- 验收仍保留一项：中文文案落地后在 `out/main/index.js` 中 grep 回并逐字比对
  ——防的是**编辑期**再次以 GBK 读写，不是构建期。
- 建议同时补一条防复发措施：`.editorconfig` 显式声明 `charset = utf-8`，
  并在 `check:boundaries` 脚本中加入乱码检测（GBK 往返 + PUA/片假名判定），
  使同类事故在 CI 阶段即被拦截，而不是等到用户看见。

---

## 5. 日志规范（W2）

### 5.1 唯一入口

`common/logger.ts` 是全后端唯一日志出口。**禁止裸 `console.*`**，由边界脚本检查
（`common/logger.ts` 与 `undici-polyfill.ts` 除外——后者在 logger 可用之前运行）。

```ts
export type LogLevel = 'debug' | 'info' | 'warn' | 'error'

export interface Logger {
  debug(message: string, fields?: LogFields): void
  info(message: string, fields?: LogFields): void
  warn(message: string, fields?: LogFields): void
  error(message: string, fields?: LogFields): void
  child(scope: string, fields?: LogFields): Logger   // 绑定模块名与常驻字段
}
```

约定：

- **结构化字段，不做字符串拼接**：`log.info('run started', { taskId, runId })`，
  不写 `` log.info(`run ${runId} started`) ``。便于过滤与后续接落盘。
- **每个模块用 `child(模块名)`**，日志自带来源，不再手写 `[TaskExecutionCoordinator]` 前缀。
- **全部字段过 `redact`**：token / cookie / password / secret / apiKey / credential 一律脱敏。
  这条与安全约束直接绑定，不是风格问题。
- 级别语义：`debug` 开发排查（生产默认关）、`info` 正常生命周期节点、
  `warn` 可恢复异常与自动降级、`error` 用例失败或不变式被破坏。

### 5.2 必须打日志的位置

现状 30 处 `console.*` 分布不合理：`pi/sdk/pi-coding-agent-adapter.ts` 用 `console.error`
打了 10 行**纯信息**日志，`pi/pi-task-worker.ts` 5 处同类。以下位置按职责重新设置，
而不是照搬现有调用点。

| 位置 | 级别 | 记录内容 | 理由 |
|------|------|----------|------|
| 应用启动完成 / 组装失败 | info / error | 版本、userData 路径 / 失败原因 | 启动问题现在完全没有痕迹 |
| 停机开始 / 完成 / 超时 | info / info / error | 在跑的 run 数、耗时 | C3 的可观测性；超时说明有 run 没收干净 |
| 认证失效（401 触发） | warn | 触发来源、被中止的 run 数 | C7 的半清理状态需要痕迹 |
| run 准入 / 启动 / 终态 | info | taskId、runId、outcome、耗时 | 任务生命周期主干 |
| 工作区锁冲突导致排队 | debug | runId、冲突路径 | C2 泄漏后的静默滞留可被发现 |
| 工作区锁释放异常 | error | runId、路径 | 直接对应 C2 |
| 队列条目被丢弃 | warn | runId、原因 | 直接对应 C1，卡死任务不再无声 |
| 审批请求 / 结果 | info | requestId、toolName、结果、是否超时 | 安全审计；**不记参数内容** |
| 审批 requestId 缺失被拒 | warn | pending 数量 | 直接对应 C5 |
| 事件落盘失败 | error | taskId、runId、错误 | 直接对应 C6 被吞掉的异常 |
| 持久化写入失败 / 走备份恢复 / 隔离损坏文件 | error / warn / warn | 文件用途（**不记完整路径**）、错误 | 数据层现在几乎不出声 |
| scope 切换丢弃在途写入 | warn | 旧 generation | 直接对应 C8 |
| 状态跃迁被拒 | warn | taskId、from、to | 直接对应 C9 |
| 平台请求失败 / 重试 | warn | 接口名、状态码 | **不记 URL 查询串与请求头** |
| 未捕获异常 / 未处理 rejection | error | 错误与栈 | 当前完全没有兜底 |

### 5.3 明确不打日志的位置

- **`text_delta` 等逐 token 事件**：不逐条打日志（可打聚合计数）。否则重演 C6 那类性能问题。
- **工具调用的参数与返回内容**：可能含用户代码与密钥，只记 toolName 与结论。
- **完整文件路径**：可能含用户目录名，记用途标识；需要路径时只记相对 scope 的部分。
- **任何 token、refresh token、请求头**：绝不记录，即便脱敏后也不记原始形态。

---

## 6. 直接删除清单（W1）

已确认无生产引用，**不搬迁、不保留、不留兼容层**。

| 目标 | 依据 |
|------|------|
| 根目录 `electron-vite.config.ts`（连字符） ✅ | electron-vite 只解析带点的 `electron.vite.config.*`，此文件从未被加载 |
| `tasks/domain/workflow-executor.ts`（`WorkflowExecutor`） ✅ | 全仓库无生产引用；`workflow:start` 走的是 coordinator 的 `executeTask` |
| `unused/task-workspaces/` ✅ | 目录名即结论（实为空目录树，未入 git） |
| `WorkspaceLockManager.waitForRelease()`（`:73-81`） ✅ | 无任何调用者；coordinator 用 `pump()` 重试而非信号等待。连带删掉已无人填充的 `waiters` 与唤醒循环 |
| `TaskManager.cancelTask()` ✅ | `main.ts` 用的是 coordinator 的 `cancelTask`；此方法只被一个测试使用，该测试已改走生产路径 |
| `task-manager.ts:338-339` 的死赋值 ✅ | 赋值后立即被下一行覆盖（随 `cancelTask()` 整体删除） |
| `task-run-store.ts` 的 `enqueue` / `enqueueValue` 之一 | 两个近乎相同的链式助手，归并为一个泛型实现 |
| `main.ts` 的 `authError()` / `taskError()` / 裸错误对象 | 由 `error-mapper.ts` 取代后删除 |
| 3 套原子写实现中的 2 套 | 由 `data/atomic-file.ts` 取代 |
| 4 处路径推导中的 3 处 | 由 `data/scope-path.ts` 取代 |
| 2 份脱敏正则与 `sanitize*` 中的 1 份 | 由 `common/redact.ts` 取代 |
| 3 处硬编码副作用工具清单（含 `coordinator.ts:13`、`task-run-store.ts:286`） | 收敛到 `common/constants.ts` 一处 |
| 约 10 处逐字复制的 scope 守卫 | 由 `service/scope-guard.ts` 取代 |
| 6 个 `ensureXxx` 惰性单例 | 由 `bootstrap/composition-root.ts` 取代（coordinator 的异步惰性加载除外，见 Phase 4） |
| 全部裸 `console.*`（30 处） | 由 `common/logger.ts` 取代 |

归并类条目的执行要求：**同一提交内删掉旧实现**，不允许出现"新实现已就位、旧实现仍在"的中间状态。

---

## 7. 并发不变式（行为基准）

从现有代码反推出的、重构前后必须保持不变的规则。行为基准文档已删除，**这份清单就是替代基准**，
Phase 0 内落成可执行断言，此后每阶段都要通过。

| # | 不变式 | 来源 | 测法 |
|---|--------|------|------|
| I1 | 同一 taskId 最多一个 active run | `activeByTask: Map<taskId, ActiveRun>` | 假 worker，确定性 |
| I2 | 重叠的工作目录（含父子关系）不得同时被两个 run 持有 | `pathsOverlap` + `locks` | 假 worker，确定性 |
| I3 | 每个 run 的事件按 sequence 严格单调递增落盘 | `appendEvent` 序号分配 | 并发压测：同时投 50 条 |
| I4 | 同一 taskId 的事件处理严格串行 | `eventChains` | 并发压测，断言处理顺序 |
| I5 | 同一 scope 的任务快照写入严格串行 | `mutationChain` + `TaskStore.writeChains` | 并发压测，断言落盘顺序 |
| I6 | `activeRunId` 非空 ⟺ 该 run 在队列中或在 active 中 | `admitTask` / `clearTaskRun` / `settleTaskRun` | 假 worker + 状态断言 |
| I7 | 停机时每个 in-flight 副作用工具都产出一条 `SIDE_EFFECT_UNKNOWN` | `finalizeRun:487-500` | 可控时钟 + 假 worker |
| I8 | 审批超时（60s）后自动拒绝，且拒绝后不可被追认 | `approval-broker` + C5 修正 | 可控时钟 |
| I9 | run 的终态只写一次 | `task-run-store.ts:328-348` 的 `transition` 前置条件 | 重复调用断言 |

现有 coordinator 的 options 已支持注入 `createWorker` 与 `taskRunStore`，I1/I2/I6/I7 可用假实现做
确定性测试，不需要真跑 pi。

---

## 8. 实施阶段

### 阶段总览

| Phase | 主题 | 风险 | 是否改行为 |
|-------|------|------|-----------|
| 0 ✅ | 安全网、编码定性、删死物 | 极低 | 否（除用户可见乱码修正） |
| 1 | **修完全部 9 个缺陷** | 中 | **是**（每个缺陷独立 commit） |
| 2 | 报错模块 + 日志模块落地 | 低 | 是（文案改中文） |
| 3 | 纯移动（`git mv`） | 低 | 否 |
| 4 | 组装根与生命周期 | 中 | 否 |
| 5 | 服务层 | 中 | 否 |
| 6 | 控制层表驱动 + zod | 中 | 否 |
| 7 | 数据层归一 | 中 | 否 |
| 8 | 拆 coordinator | 高 | 否 |

**Phase 0-6 完成即达到"可继续加功能"状态**，7、8 可按需推进。

---

### Phase 0 — 安全网与前置定性 ✅ 已完成（2026-09-02）

结构一行不动。这是后续所有阶段的行为基准，不可跳过。

- [x] **判定乱码成因**——已完成，结论见第 4.4 节：构建链干净，损坏是 `6006e9f` 一次性事故，
      原文可从 `763e51e` 精确恢复。**第 2 阶段中文文案可行。**
- [x] 按第 4.4 节的恢复表修复乱码。实际修复量比初次统计更多：加入 `€`(0x80) 映射后
      又发现 `src/shared/types.ts` 6 处分隔线损坏，全仓库最终修复 **5 个文件 / 25 处运行**：
      `main.ts` 重复损坏的文件头合并为一份（同时清掉多余的 `/*`）、
      `main.ts:229/292/294/395/434/785/811` 分隔线、`main.ts:795`「选择工作目录」、
      `main.ts:815` 注释、`task-execution-coordinator.ts:576/580`「执行工具: 」「工具执行完成: 」、
      `task-manager.ts:298`「生成文件: 」。
      `task-manager.ts:338` 的死赋值随 `TaskManager.cancelTask()` 一并删除。
      `src/shared/types.ts` 改为**一并修复**：它是主进程编译进来的 IPC 契约文件，
      留着已知乱码而同时上线乱码检查规则自相矛盾；改动仅注释与分隔线，零行为风险。
- [x] 加 `.editorconfig` 显式声明 `charset = utf-8`（并统一 `end_of_line = crlf`）。
- [x] `check:boundaries` 加入乱码检测：运行时反查 GBK 双字节表（含 `€`→0x80 与私用区），
      按 GBK 编回再当 UTF-8 读，命中且结果像源码即报错；另判私用区与片假名。
      已反向验证过能拦住。
- [x] `test:tasks` 改为覆盖全部后端测试文件（`workflow-graph.test.ts` 此前从未运行过，
      现已纳入且通过；`workflow-executor.test.ts` 随实现一并删除）。
      **另加一条 enforced 规则 `test:every-suite-registered`**：读 `package.json` 的
      `test:tasks` glob，断言 `electron/` 下每个 `*.test.ts` 都被覆盖，防同类漏跑复发。
- [x] `preload.ts` 的 `as ElectronAPI` 改为 `satisfies ElectronAPI`。
- [x] 删死物：`electron-vite.config.ts`（连字符，从未被加载）、`WorkflowExecutor` 及其测试、
      `unused/`、`WorkspaceLockManager.waitForRelease()`（连带已无人填充的 `waiters` 集合与
      唤醒循环）、`TaskManager.cancelTask()`。
      后者原先只有一个测试在用，该测试改为走生产路径（`settleTaskRun(PENDING)`，
      即 coordinator 取消排队 run 的实际做法），覆盖不减。
- [x] 建 `electron/controller/channels.ts`，33 个通道常量分 `INVOKE` / `SEND` / `EVENT` 三组，
      `main.ts` 与 `preload.ts` 都引用。纯替换字面量，未改任何 channel 名。
      `task-manager.ts` 的 2 处 `webContents.send` 暂留字面量——那两处调用点本身要在
      Phase 5 迁出数据层，此时让数据层反向依赖 `controller/` 不合理。
- [x] 建 IPC 契约快照测试 `electron/controller/ipc-contract.test.ts`：用 TypeScript 编译器 API
      读语法树（preload 在模块加载时就调 `contextBridge`，无法在 Electron 外 import），
      断言 `ElectronAPI` 成员集合 ≡ preload 暴露键集合、通道名唯一、
      每个 invoke/send 通道恰有一个注册、preload 订阅了全部 event 通道、preload 无裸字面量。
- [x] 建 `scripts/check-boundaries.ts`（B1-B4 + 禁止裸 `console.*`），分层规则标记为 pending
      并注明各自在哪个阶段转 enforced；编码与测试注册两类规则**立即 enforced**。
- [x] **建第 7 章 I1-I9 的不变式测试** `electron/tasks/concurrency-invariants.test.ts`，
      注入假 worker / 假 store，11 个用例全绿。断言的是"今天已成立"的性质；
      各缺陷触发条件下的回归测试属 Phase 1。
- [x] *计划外补做*：恢复 `eslint.config.js`。该文件只存在于未合入的旁支（`3403532`/`6f71a16`），
      当前分支上 `npm run lint` 一直是直接报错退出的——一个宣称存在却跑不起来的检查不算安全网。
      恢复后仅 2 个 error（`task-execution-coordinator.ts` 两处应为 `import type`），已修；
      余 3 个 warning 全在渲染进程 `WorkspaceComposer.tsx`，属前端，本轮不动。

验收：`typecheck` + `lint` + `test:tasks`(65) + `test:invariants`(17) + `check:boundaries` +
`build` + `poc:01..04` 全绿。构建产物已逐字比对：三个主进程/preload 产物均为合法 UTF-8、
私用区字符数 0，`选择工作目录`/`执行工具: `/`工具执行完成: `/`生成文件: ` 均原样出现。

---

### Phase 1 — 修完全部缺陷（W3）

**结构一行不动**，只改逻辑。每个缺陷一个 commit，信息形如 `fix(coordinator): 队列按 runId 寻址 (C1)`。

- [ ] **C1** `pump()` 与 `removeQueuedTask()` 全部改为按 `runId` 寻址；
      补测试：`pump` 挂起期间调用 `cancelTask`，断言其他排队条目未被误删（对应 I6）。
- [ ] **C2** `startRun` 进入函数后立即 `try`，`getPaths()` 与 `createWorker()` 移入 try 内，
      `releaseWorkspace()` 只在 `finally` 调用；
      补测试：让 `createWorker` 抛错，断言锁已释放、后续同目录任务能启动（对应 I2）。
- [ ] **C3** 改 `before-quit` + `preventDefault` 两阶段退出 + `withTimeout(5000)` + `app.exit(0)`；
      同时修 `whenReady` 里未 await 的 `ensureTaskManager()`；
      补测试：I7（停机时 in-flight 副作用产出 `SIDE_EFFECT_UNKNOWN`）。
- [ ] **C4** 授权移到入队前完成一次，`EmployeeRuntimeConfig` 随队列条目携带；
      调度循环内禁止任何网络调用；平台目录读取加 TTL 缓存 + 单飞去重。
      *此阶段不建新目录，只在现有文件内拆能力；正式落成 `service/` 两个文件在 Phase 5。*
- [ ] **C5** `approval-broker.respond()` 要求 `requestId`；缺失时仅在"恰好一个 pending 且
      taskId/runId 匹配"时回退，否则返回 false 并记 warn；
      补测试：I8（A 超时拒绝后 B 进入 pending，无 requestId 的响应不得批准 B）。
- [ ] **C6** 事件序号改为 per-(task, run) 内存计数器 + 首次从文件尾部 seek 初始化；
      `enqueueEvent` 的链条错误不再吞，交错误上报；提供显式 `drain(taskId)`；
      补测试：I3（并发投 50 条事件，断言 sequence 严格单调）。
- [ ] **C7** `stopAll` / 停机 / `invalidateAuthentication` 全部套 `withTimeout`，超时记 error；
      认证失效引入显式 invalidating 状态，期间 scope 守卫直接返回 `AUTH_REQUIRED`。
- [ ] **C8** scope 切换纳入 `mutationChain`；引入 `generation`，`commit` 的 `submit` 内校验
      generation 未变，否则放弃写入并记 warn；
      补测试：I5（切换 scope 期间的在途 commit 不得写回旧 scope）。
- [ ] **C9** 所有守卫收进 `commit` 的 mutate 闭包内，唯一检查点；
      `InvalidTaskTransitionError` → `INVALID_STATE`；清 `task-manager.ts:338-339` 死赋值。

验收：每个 commit 后跑 `typecheck` + `test:tasks` + `test:invariants`；
阶段结束加跑 `build` + `poc:01..04` + **真机冒烟**（见第 10 章）。

> C1/C2/C6/C5 的最终归位在 Phase 8（拆 coordinator）。Phase 1 只在现有文件内修正逻辑，
> 不预先拆类——先让缺陷消失，再动结构。

---

### Phase 2 — 报错模块与日志模块

只新增文件 + 替换调用点，不移动任何模块。

- [ ] 建 `errors/` 四个文件（第 4 章）。
- [ ] `authError()` / `taskError()` / `main.ts:413` 裸对象 / `main.ts:430` 全部改走 `error-mapper`，
      **随即删除旧实现**（W1）。
- [ ] 建 `common/logger.ts` + `common/redact.ts` + `common/with-timeout.ts`
      （`with-timeout` 若 Phase 1 已需要则提前建，位置不变）。
- [ ] 归并 2 份脱敏实现为 `common/redact.ts` 一份，删旧的。
- [ ] 按第 5.2 节重新设置日志点，删除全部 30 处裸 `console.*`；
      开启边界脚本的"禁止裸 `console.*`"规则。
- [ ] 注册 `unhandledRejection` / `uncaughtException` 兜底。
- [ ] 初始化失败改走 `dialog.showErrorBox` 中文提示。
- [ ] **中文文案全量落地**。第 4.4 节已确认构建链按 UTF-8 透传、不做转码，因此无需额外方案；
      仍在 `out/main/index.js` 中 grep 回中文并逐字比对，作为编辑期回归的哨兵。
- [ ] grep 渲染进程是否对错误 `message` 做英文匹配；若有，列出位置交你决定，本轮不改前端。

验收：全套 + 构建产物中文逐字比对。

---

### Phase 3 — 纯移动

按第 3.2 节 `git mv`（含 `auth/` → `common/platform/` 与文件改名），**只改 import**。
测试文件跟随源文件移动。

审 diff 只看一件事：**非 import 行的改动数 = 0**。

验收：全套 + `poc:01..04`（SDK 加载边界唯一的自动化验证，此阶段不能跳）。

---

### Phase 4 — 组装根与生命周期

- [ ] `bootstrap/composition-root.ts` 的 `createBackend()` 替掉 10 个全局 + 6 个 `ensureXxx`，
      依赖显式传入构造函数。
- [ ] **保留唯一的异步惰性加载**：`async getTaskCoordinator()` 内部维持动态 `import`。
      CLAUDE.md 那条 Electron 33 / undici 边界不得破坏——`common/undici-polyfill` 必须是首个副作用
      import，`TaskExecutionCoordinator` 必须 `import type`，真实加载必须是异步边界之后的动态 import。
      破坏它会以 `markAsUncloneable is not a function` 崩溃启动。
- [ ] 顺带修 `ensureTaskCoordinator()` 的并发等待漏洞：初始化失败后，并发等待者会观察到
      `taskCoordinator!` 实际为 undefined。改为等待者共享同一 promise 的结果值。
- [ ] `app.whenReady()` 改为 `await createBackend()` 后再开窗；失败弹中文错误框。
- [ ] `bootstrap/shutdown.ts` 收纳 Phase 1 的 C3 实现。
- [ ] 引入 `runtime/task-notifier.ts` 接口，把 `task-manager.ts:415-426` 的 `webContents.send`
      换成注入的 notifier——**B1 边界在此转为强制**。

验收：全套 + `poc:01..04` + 真机冒烟。

---

### Phase 5 — 服务层

- [ ] 27 个 IPC 回调的用例逻辑搬进 `service/`；回调只剩"校验 → 调服务 → 转信封"三步。
- [ ] `service/scope-guard.ts` 归并约 10 处重复守卫，删旧的。
- [ ] Phase 1 的 C4 能力正式落成 `service/employee-directory.ts`（TTL + 单飞，唯一平台目录读取点）
      与 `service/employee-authorizer.ts`（纯函数，目录快照入参）；
      `resolveEmployee` 改读显式入参，**全局 `activeInstances` 删除**。
- [ ] `task:create` 与 `conversation:create` 的公共部分下沉到同一服务方法；
      两个 channel 保留为薄入口（渲染进程在用 `task:create`，**不能删**）。
- [ ] `common/constants.ts` 收敛 3 处副作用工具清单。
- [ ] 开启 B2 边界规则。

验收：全套 + `poc:01..04`。

---

### Phase 6 — 控制层表驱动 + zod

- [ ] 加 `zod` 为生产依赖，**pin 精确版本（不用 `^`）**，只进主进程、不进渲染进程包。
- [ ] `controller/router.ts` 统一做五件事：查表注册、跑 zod（失败 → `INVALID_ARGUMENT`）、
      注入 scope、调 handler、catch 后交 `error-mapper`。
- [ ] 33 条 route 填入 `controller/routes/*.routes.ts`。

route 形态（zod 自动推导 handler 入参类型，不用手写）：

```ts
export const taskRoutes = [
  route(CH.TASK_CREATE, z.object({
    title: z.string().min(1).max(200),
    prompt: z.string().min(1),
    workDir: z.string().nullish(),
    subscriptionId: z.string().min(1),
  }), (ctx, input) => ctx.taskService.create(ctx.scope, input)),
]
```

**不填 schema 就注册不了**——这把项目约束"校验所有渲染进程传入的参数"
从"靠人记得"变成结构强制。

- [ ] `main.ts` 收敛到 120 行内：polyfill → 组装 → 注册表 → 窗口生命周期，无任何业务判断。
- [ ] 开启 B1 边界全部规则。

验收：全套 + IPC 契约快照测试 + 真机冒烟。

---

### Phase 7 — 数据层归一

- [ ] `data/atomic-file.ts` 归并 3 套原子写（保留 `task-store` 的 `.bak` + `quarantine` 语义为默认行为），
      删旧实现。
- [ ] `data/scope-path.ts` 归并 4 处路径推导，删旧实现；
      清掉 `getPaths(scope, taskId, 'store')` / `'conversation-session'` 这种借位 runId 的路径 hack。
- [ ] 从 `task-run-store` 拆出 `task-event-store`（收纳 Phase 1 的 C6 内存计数器）
      与 `task-message-projector`；`task-run-store` 只留 run 记录。
- [ ] 归并 `enqueue` / `enqueueValue` 为一个泛型实现。
- [ ] 开启 B3 边界规则。

验收：全套。**额外**：用旧数据目录启动，确认历史任务、run、事件、消息全部可读
（持久化格式与目录布局不变是硬约束）。

---

### Phase 8 — 拆 coordinator（风险最高）

前置条件：I1-I9 全部有测试，且 Phase 0-7 全程绿。

- [ ] 按 5 个职责拆出 `admission-queue`（收纳 C1 修正）、`worker-registry`（收纳 C2 修正）、
      `event-pipeline`（收纳 C6 的 `drain()`）、`workspace-lock-manager`、
      `approval-runtime`（收纳 C5 修正），coordinator 退化为编排者。
- [ ] coordinator 的 8 个公开方法签名不变。
- [ ] 开启 B4 边界规则；边界脚本全部规则转为强制。

验收：全套 + `poc:01..04` + 真机冒烟 + I1-I9 全绿。

---

### 为什么是这个顺序

- **Phase 0 → 1**：先有基准断言，才敢改并发逻辑。反过来会无从判断修复是否引入回归。
- **Phase 1 最先修完缺陷**（W3）：C1/C2/C3/C5 是当前已存在的缺陷，修复可以立即合并，
  不必等整轮重构；也避免它们在重构中期发作、与结构回归混淆。
- **Phase 2 报错与日志早于搬迁**：后面每层搬迁都要往里抛错、打日志；先有统一出口，
  搬迁过程中就不会长出第四种信封或第 31 处 `console`。
- **Phase 3 纯移动零风险**、可随时中止。
- **Phase 4 之后依赖显式**，Phase 5 的服务层搬迁才有落点。
- **Phase 8 最后**：它触碰的是最复杂的并发状态，此时缺陷已修完、不变式已有测试、依赖已显式，
  是风险最低的时机。

---

## 9. 扩展性验收

以"给任务加标签"为例，对比第 1.5 节的现状。

**重构后**：

```
新建 service/task-tag-service.ts                ← 1 个新文件
data/task-metadata-store.ts                     ← 加 2 个方法
controller/routes/task.routes.ts                ← 加 1 行 route(...)
controller/channels.ts                          ← 加 1 行常量
（前端需要调用时）src/shared/ipc.ts + preload.ts ← 各加 1 行
```

`main.ts` 零改动，错误处理零改动，scope 校验零改动，并发保护零改动，日志零额外配置
（`logger.child('task-tag')` 即可）。

**新增一种失败情形 = `errors/error-codes.ts` 加一行**，文案、状态码、可重试性、日志级别同时确定。

这就是原则 3 的兑现：因为每种判断只有一个定义点，所以新增功能的改动点是**可枚举且最小的**。

---

## 10. 验收标准

### 10.1 每阶段必跑

```bash
npm run typecheck          # tsc --noEmit + tsc -p tsconfig.node.json --noEmit
npm run test:tasks         # Phase 0 起覆盖全部后端测试
npm run test:invariants    # 新增：第 7 章 I1-I9
npm run check:boundaries   # 新增：B1-B4 + 禁止裸 console.* + 乱码检测
npm run build
npm run poc:01 && npm run poc:02 && npm run poc:03 && npm run poc:04
```

`poc:01..04` 是 SDK 加载边界唯一的自动化验证。**Phase 3、4、8 后尤其不能跳过。**

### 10.2 补充判据

- IPC 契约快照测试全程通过（Phase 0 建立）。
- Phase 3 的 diff 中非 import 行改动数 = 0。
- 中文错误文案在构建产物中逐字比对无乱码。
- Phase 7 后用旧数据目录启动，历史数据全部可读。
- 全仓库 `console.` 出现次数 = 0（`common/logger.ts` 与 `undici-polyfill.ts` 除外）。

### 10.3 真机冒烟

覆盖自动化测不到的主进程行为。Phase 1、4、6、8 后各做一次：

1. 登录 → 员工列表加载。
2. 新建工作并执行。
3. 触发一个需审批的工具调用 → 批准 → 确认执行。
4. 再触发一个 → 让它超时（60s）→ 确认自动拒绝，且此后点批准无效（**C5**）。
5. 取消任务 → 确认状态与 run 终态正确。
6. 退出应用 → 确认 `SIDE_EFFECT_UNKNOWN` 已落盘（**C3**）。
7. 重启 → 历史可读，**无卡死任务**（**C1**），同一工作目录可再次启动任务（**C2**）。

---

## 11. 明确不做的事

- 不升级 `@earendil-works/pi-coding-agent` / `@earendil-works/pi-ai`（锁定 `0.83.0`）。
- 不改任何 IPC channel 名字。
- 不改持久化文件格式与目录布局——旧数据必须能直接读。
- 不改任务状态机语义（除 C9 的错误码映射修正）。
- 不重构 `common/platform/` 内部（只搬不改）；`credential-vault.ts` 的模块作用域同步 fs
  与 import 期读 `app.getPath` 留待下一轮。
- 不修 `npm run lint`（ESLint 9 配置迁移）——因此"lint 通过"目前不是有效验收项，
  由 `check:boundaries` 替代。
- 不给 `WorkspaceLockManager` 加跨进程锁文件，不加 `requestSingleInstanceLock`（属新增能力）。
- 不改 `src/shared/ipc.ts` / `src/shared/types.ts` 与任何渲染进程代码（本轮忽视前端）。
- 不清理 `docs/对接/测试账号.md` 的 git 历史，需单独确认。
